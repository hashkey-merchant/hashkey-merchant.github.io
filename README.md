# HashKey Merchant — static docs

## Run locally

```bash
python3 -m http.server 8000
```

Open:

- Hong Kong Traditional Chinese (default): `http://localhost:8000/index.html`
- English: `http://localhost:8000/en/index.html`

Markdown sources: `docs/zh-HK/`, `docs/en/`.
